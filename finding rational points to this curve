Q := Rationals();

// Define the curve
E := EllipticCurve([0, 36/19, 36, 228, 360]);

// Compute torsion subgroup
TorsionSubgroup(E);

// Compute rank and generators (if any)
RankBounds(E);
Generators(E);

// Search for rational points up to a bound
RationalPoints(E : Bound := 1000);
