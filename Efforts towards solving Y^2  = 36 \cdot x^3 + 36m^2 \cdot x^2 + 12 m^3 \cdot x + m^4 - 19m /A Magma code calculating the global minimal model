Q := Rationals();
R<m> := FunctionField(Q);

// Define the elliptic curve via a-invariants
E := EllipticCurve([
    0,
    m^2,
    0,
    (1/3)*m^3,
    (m^4 - 19*m)/36
]);

// Compute global minimal model
Emin := MinimalModel(E);

Emin;
